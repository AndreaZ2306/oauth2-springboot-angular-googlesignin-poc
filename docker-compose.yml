version: "3.8"

services:
  resource-server:
    build:
      context: ./resource-server
    container_name: resource-server
    ports:
      - "8081:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
